<!DOCTYPE html>
<html>
  <head>
  </head>

  <body>
    <h1>Books</h1>
    
    <table>　<!--ここからテーブル作成 -->
      <thead> <!-- 表のヘッダを表す要素（テーブルに設定できるのは一つだけ）-->
        <tr>
          <th>Title</th>
          <th>Body</th>
          <th colspan="3"></th> 　<!-- 1つのセルとしてまとめるカラム数1以上の整数指定 -->
        </tr>
      </thead>
      <tbody> <!-- 表の本体部分-->
        <% @books.each do |book| %>  <!-- ここから行をループして表示｜bookメソッドにeachで値を入れて繰り返している｜-->
        <tr>
          <td><%= book.title %></td>
          <td><%= book.body %></td>
          <td><%= link_to 'show', show_book_path(book.id) %></td>
          <td><%= link_to 'Edit', edit_book_path(book.id) %></td>
          <td>ここにDestroy</td>
        </tr>
        <% end %>
      </tbody>
    </table>
    
    <h4>New book</h4>
    <!-- from_with部分-->
    <%= form_with model: @book, local:true do |f| %>
    
      <div class = "field">
        <label>Title</label>
        <%= f.text_field :title %>
      </div>
      <div class = "field">
        <label>Body</label>
        <%= f.text_area :body %>
      </div>
      <p>
      <%= f.submit 'Create' %>
      </p>
      
    <% end %>
  </body>
</html>